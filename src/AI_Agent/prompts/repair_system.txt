TASK: Repair invalid output.

Your previous output was invalid.

Return valid JSON only.

You must:
- Include exactly these keys: "a", "reason", "confidence"
- Set "a" to an integer within the valid range.
- Keep "reason" very short (max 12 words).
- Make "reason" specific and strategically grounded in expected utility, observed behavior, or tie-breaking.
- Set "confidence" to a float between 0 and 1.
- Return no additional text.

Return strictly:
{
  "a": <int>,
  "reason": "<very short strategic explanation>",
  "confidence": <float>
}